# Core async HTTP + scraping
httpx>=0.27
playwright>=1.44
beautifulsoup4>=4.12
# API BetConstruct "Swarm" (Golcash) : WebSocket
websockets>=12.0

# Normalisation
rapidfuzz>=3.9

# IA (provider-agnostique ; Anthropic par defaut)
anthropic>=0.34
pydantic>=2.7
pydantic-settings>=2.3

# Storage
SQLAlchemy>=2.0
aiosqlite>=0.20
# Postgres (utilise par docker-compose ; SQLite reste le defaut hors Docker)
asyncpg>=0.29

# Dashboard
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1

# Export
openpyxl>=3.1

# Tests
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21
